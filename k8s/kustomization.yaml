apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - app-config.yaml
  - observability-prometheus.yaml
  - observability-grafana.yaml

secretGenerator:
  - name: db-credentials
    envs:
      - .env.secret


